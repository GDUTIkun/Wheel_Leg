问题描述：现在加入lqr后hip力矩输出特别小，无论我phi偏移多少都输出很小的力矩，应该是vmc的雅可比矩阵定义错了
我的腿部就是一个简单的二连杆，有大腿和水平轴的夹角以及小腿和大腿的夹角（knee夹角），和大腿、小腿长度就能计算出雅可比矩阵
现在的情况是，腿长控制的knee关节力矩能够正常输出，我length有偏移，经过pid计算后输入到vmc能输出合适的力矩来让length恢复。
但是现在我偏移phi，hip输出力矩很小，不能使摆杆恢复。
前面已经验证过不是lqr增益的问题

要求：
帮我找到正确的vmc映射，stm层是正常运行的，你直接用终端跑代码，直到成功为止。
必须关闭力矩输出，即command_enable:=false。只去观察hip和knee输出是否符合逻辑。
我现在length_c>length_target，所以knee应该输出较大的负力矩；phi_c<phi_target，所以hip应该输出较大的正力矩。

树莓派的密码是一个空格。